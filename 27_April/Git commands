Git commands
git switch -c master
git config --global user.email "anuritha.p@gmail.com"
git config --global user.name "Shyama"
git add .
git push origin master
git commit -m "comment"

%%writefile app.py
!python app.py

Serving a model:
REST API : FAST API, Tensorflow serving
Web Application: streamlit
Torchserve, AWS sagemaker

streamlit
streamlit run app.py & npx localtunnel --port 8501

FastAPI
uvicorn app:app --reload
http://127.0.0.1:8000/docs

Terminal1: uvicorn app:app --host 0.0.0.0 --port 8000
Terminal2: npx localtunnel --port 8000


Pytest:
pip -q install pytest pytest-sugar
!python -m pytest test_math.py